{{- if eq .chezmoi.os "darwin" -}}
# homebrew/cask-fonts is deprecated, fonts are now in homebrew/cask
cask "font-fira-code-nerd-font"
# Install opencode
tap "anomalyco/tap"
brew "opencode"
{{- end }}

{{- if eq .chezmoi.os "darwin" }}
# macOS gets all the tools via brew (bottles usually available)
brew "ripgrep"
brew "bat"
brew "eza"
brew "fzf"
brew "zoxide"
brew "fd"
brew "tldr"
{{- end }}

brew "chezmoi"
# Bun is installed via curl in run_onchange_install-bun.sh.tmpl

{{- if eq .chezmoi.os "linux" -}}
# Linux specific dependencies
tap "anomalyco/tap"
brew "opencode"

# Heavy Rust-based CLI tools are temporarily disabled on Linux to prevent CI hangs/timeouts
# caused by compilation from source. Use apt or manual binaries if needed.
# brew "ripgrep"
# brew "bat"
# brew "eza"
# brew "fzf"
# brew "zoxide"
# brew "fd"
# brew "tldr"
{{- end -}}
