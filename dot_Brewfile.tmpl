{{- if eq .chezmoi.os "darwin" -}}
tap "homebrew/cask-fonts"
cask "font-fira-code-nerd-font"
# visual-studio-code is often useful, but user specifically asked for opencode
# We assume opencode is available via brew as requested.
tap "anomalyco/tap"
cask "anomalyco/tap/opencode"
{{- end }}

brew "ripgrep"
brew "bat"
brew "eza"
brew "fzf"
brew "zoxide"
brew "fd"
brew "tldr"
brew "chezmoi"
# Bun is installed via curl in run_onchange_install-bun.sh.tmpl

{{- if eq .chezmoi.os "linux" -}}
# Linux specific dependencies if any
# opencode via brew on linux
tap "anomalyco/tap"
brew "anomalyco/tap/opencode"
{{- end -}}
