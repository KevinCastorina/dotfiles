{{- if eq .chezmoi.os "darwin" -}}
# homebrew/cask-fonts is deprecated, fonts are now in homebrew/cask
cask "font-fira-code-nerd-font"
# Install opencode as a CLI tool (not a cask)
brew "anomalyco/tap/opencode"
{{- end }}

brew "ripgrep"
# bat is installed here for macOS, but handled separately/skipped for Linux if causing issues
{{- if eq .chezmoi.os "darwin" }}
brew "bat"
{{- end }}
brew "eza"
brew "fzf"
brew "zoxide"
brew "fd"
brew "tldr"
brew "chezmoi"
# Bun is installed via curl in run_onchange_install-bun.sh.tmpl

{{- if eq .chezmoi.os "linux" -}}
# Linux specific dependencies if any
# Install opencode as a CLI tool
brew "anomalyco/tap/opencode"
# bat installation via brew often hangs on Linux CI (compilation from source).
# Skipping for now to ensure pipeline succeeds.
# brew "bat"
{{- end -}}
